# 候选事项

这里保存尚未确定开发的建议，不表示已经排期或获得代码修改授权。确认开发后，将事项移入 [ROADMAP](ROADMAP.md)，并按风险、依赖和决策复杂度决定是否建立活动方案；不在两处重复维护当前状态。

## 局部统一组件和样式

来源：[框架与组件使用规范调整方案](plans/completed/20260905-framework-component-alignment.md)的阶段 B。当前主题、CSS 与 wrapper 分工已经在 [DESIGN](DESIGN.md) 中说明，但这不意味着还必须继续重构。

启动前需要具体消费者证据：例如重复默认值、重复产品约束或确实造成维护问题的样式定义冲突。提出调整时说明影响文件、保留行为、收益与验证范围，再取得实施授权。

不为了统一形式包装全部 Mantine API、不全量重写 CSS，也不借此实施素靛主题。没有实际问题时不开发。

## 按具体维护问题调整状态所有权

来源：[框架与组件使用规范调整方案](plans/completed/20260905-framework-component-alignment.md)的阶段 C。当前所有权已在 [ARCHITECTURE](ARCHITECTURE.md) 解释；是否抽取模块取决于真实维护问题，而不是 `PwaApp` 的行数或 `useState` 数量。

启动前应定位连接、配对或时间线的具体职责冲突及直接消费者影响，再判断仅补充说明还是局部实现。没有问题证据时保持现状；不默认引入新的状态管理库，也不借此改变协议、持久化或 daemon/Relay 边界。

已确认但未结束的主题和设备验收事项只在 ROADMAP 跟踪，不在这里重复列项。旧方案中的历史待办不会自动进入本文件。
